- name: demo on loops
  hosts: localhost
  tasks:
    - name: print names
      ansible.builtin.debug:
        msg: "{{ item }}" # Corrected "items" to "item"
      loop:
        - jenkins
        - ansible
        - docker